/**
 * Contains our custom servlets. These are used for managing
 * downloads and for enforcing access privileges when users
 * retrieve PDFs (scripts or reports).
 */
package com.lightspeedeps.web.servlet;
